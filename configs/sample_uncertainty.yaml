seed: 42

data:
  root_dir: data/target/ixmtest
  train_ratio: 0.9
  patch_size: 128
  upscale_factor: 4

model:
  base_channels: 64
  t_dim: 256

diffusion:
  timesteps: 1000
  beta_start: 0.0001
  beta_end: 0.02

sample:
  checkpoint: checkpoints/fewshot/lora_k20.pt
  n_examples: 8
  n_samples: 16
  output_dir: outputs/uncertainty
  ddim_steps: 50
  eta: 1.0
