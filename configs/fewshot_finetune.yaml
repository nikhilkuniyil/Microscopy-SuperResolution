seed: 42

data:
  root_dir: images
  train_ratio: 0.9
  patch_size: 128
  upscale_factor: 4
  few_shot_k: 20

model:
  base_channels: 64
  t_dim: 256

diffusion:
  timesteps: 1000
  beta_start: 0.0001
  beta_end: 0.02

lora:
  rank: 4
  alpha: 1.0

train:
  base_checkpoint: checkpoints/base/epoch_099.pt
  batch_size: 4
  num_workers: 0
  lr: 0.00005
  weight_decay: 0.0
  iterations: 500
  checkpoint_dir: checkpoints/fewshot
